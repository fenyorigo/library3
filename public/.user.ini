; BookCatalog upload/import limits (persistent per-app settings)
; Applies with PHP-FPM/CGI when user_ini is enabled.

upload_max_filesize = 512M
post_max_size = 540M
memory_limit = 512M
max_execution_time = 600
max_input_time = 600
